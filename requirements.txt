fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12

# Document extraction
pymupdf==1.24.11
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5
pandas==2.2.3

# Embeddings
sentence-transformers==3.2.1
# NOTE: torch is intentionally NOT listed here. It's installed separately
# in the Dockerfile from PyTorch's CPU-only wheel index, which skips the
# ~1.5-2GB of NVIDIA CUDA dependencies (cublas, cudnn, cufft, etc.) that
# the default PyPI torch wheel pulls in. We only run CPU inference on a
# small embedding model, so the CUDA libraries provide zero benefit here.
# If running this file directly (not via Docker), install torch first with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Vector DB
psycopg[binary]==3.2.3
pgvector==0.3.5
sqlalchemy==2.0.35

# LLM
groq==0.11.0
# Pinned: groq 0.11.0 internally passes a 'proxies' kwarg when constructing
# its httpx client, which httpx removed in 0.28.0. Without this pin, pip
# resolves httpx to the newest version (since nothing else constrains it
# tightly enough), causing "Client.__init__() got an unexpected keyword
# argument 'proxies'" at runtime when the Groq client is first created.
httpx==0.27.2

# Observability (optional, no-op if not configured)
# Pinned to v3.x: uses LANGFUSE_HOST env var and get_client()/
# start_as_current_observation() API. v4.x renames LANGFUSE_HOST to
# LANGFUSE_BASE_URL -- if upgrading, update app/config.py and
# app/rag_pipeline.py's env var handling accordingly.
langfuse==3.14.3

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0
tqdm==4.66.5
